use std::{thread::sleep, time::Duration};

fn main(){
           // An idea: we keep the same state object on the heap
        // and the state transitions change what access patterns exist 
        // In this kind of setup
        
       
}